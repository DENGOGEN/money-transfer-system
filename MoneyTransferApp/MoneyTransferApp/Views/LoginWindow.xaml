<Window x:Class="MoneyTransferApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Money Transfer - Вход" Height="450" Width="500"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Система денежных переводов" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <TextBlock Grid.Row="1" Text="Номер телефона" 
                   FontSize="14" Margin="0,5,0,5"/>
        <TextBox x:Name="PhoneBox" Grid.Row="2" 
                 FontSize="14" Padding="8" 
                 Margin="0,0,0,10"
                 ToolTip="Введите номер телефона в формате +7XXXXXXXXXX"/>

        <TextBlock Grid.Row="3" Text="PIN-код" 
                   FontSize="14" Margin="0,5,0,5"/>
        <PasswordBox x:Name="PinBox" Grid.Row="4" 
                     FontSize="14" Padding="8" 
                     Margin="0,0,0,10"
                     ToolTip="Введите 4-значный PIN-код"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,20,0,10">
            <Button x:Name="LoginButton" Content="Вход" 
                    Width="120" Height="40" Margin="10"
                    Background="#0078D4" Foreground="White"
                    FontSize="14" FontWeight="Bold"
                    Click="LoginButton_Click"/>
            <Button x:Name="RegisterButton" Content="Регистрация" 
                    Width="120" Height="40" Margin="10"
                    Background="#2ECC71" Foreground="White"
                    FontSize="14" FontWeight="Bold"
                    Click="RegisterButton_Click"/>
        </StackPanel>

        <TextBlock x:Name="ErrorText" Grid.Row="6" 
                   Text="" Foreground="Red" 
                   HorizontalAlignment="Center" 
                   FontSize="12" Margin="0,10"/>
    </Grid>
</Window>